# 📰 BeyondChats – AI Article Recreator (Gemini Powered)

BeyondChats is a full-stack application that searches the web for relevant articles, selects the top two websites using Google Search, fetches their content, and recreates the articles using Google Gemini LLM.

The project is structured into **frontend** and **backend**, both running independently using `npm run dev`.

---

## 📁 Project Structure

```
beyondchats/
│
├── backend/
│   ├── package.json
│   ├── .env
│   └── src/
│
├── frontend/
│   ├── package.json
│   ├── .env
│   └── src/
│
└── README.md
```

---

## ⚙️ Tech Stack

### Frontend
- React (Vite)
- Axios
- JavaScript

### Backend
- Node.js
- Express.js
- MongoDB
- Google Serper API (Search)
- Google Gemini LLM

---

## 🚀 Features

- 🔍 Uses Google Search (Serper API) to find top 2 relevant websites
- 🌐 Fetches article content from selected websites
- 🤖 Recreates articles using Gemini LLM
- 📦 Clean frontend–backend separation
- ⚡ Easy local and cloud deployment

---

## 🛠️ Installation & Setup

### 1️⃣ Clone the Repository

```bash
git clone <your-repository-url>
cd beyondchats
```

---

## 🔧 Environment Variables

### 📌 Frontend `.env` (Example)

```env
VITE_API_BASE_URL="https://beyondchatsassignment-3fj6.onrender.com/api"
```

---

### 📌 Backend `.env` (Example)

```env
PORT=5000

MONGO_URI="mongodb+srv://gandhisir80:KvcMYx9UsfbiqD6B@cluster0.tmfcp.mongodb.net/beyondChats?retryWrites=true&w=majority&appName=Cluster0"

SERPER_API_KEY=d9ed72ab6fa73cd0d973240c06f4f9ee25d78386

GEMINI_API_KEY=AIzaSyB0HCP84VL7XchtOvBj6-AJiQmsbE7NJSg
```

⚠️ These values are shown only as examples. Do not expose real credentials in production.

---

## ▶️ Running the Project Locally

### 2️⃣ Start Backend

```bash
cd backend
npm install
npm run dev
```

Backend runs on:

```
http://localhost:5000
```

---

### 3️⃣ Start Frontend

```bash
cd frontend
npm install
npm run dev
```

Frontend runs on:

```
http://localhost:5173
```

---

## 🔁 Application Flow

1. User enters a query
2. Backend uses Google Serper API to find top 2 websites
3. Articles are fetched and cleaned
4. Content is recreated using Gemini LLM
5. Rewritten articles are sent to frontend and displayed

---

## 🔗 Live Project Link

```
https://beyondchatsassignment-3fj6.onrender.com
```

---

## 👨‍💻 Author

**Mradul Gandhi**
